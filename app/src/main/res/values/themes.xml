<resources>
    <!-- Base application theme. -->
    <style name="Theme.FFCalculatorMobile" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <!-- colorPrimary - Couleur principale de l'app (barre d'app, boutons, etc.). Elle définit l’identité visuelle principale -->
        <!-- Couleur principale (barre d'app, boutons principaux) -->
        <item name="colorPrimary">@color/bleuFonce</item>
        <item name="colorPrimaryVariant">@color/bleuFonce</item>
        <item name="colorOnPrimary">@color/blanc</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/bleuClair</item>
        <item name="colorSecondaryVariant">@color/bleuClairVariant</item>
        <item name="colorOnSecondary">@color/blanc</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/bleuFonce</item>
        <!-- Customize your theme here. -->
    </style>
</resources>